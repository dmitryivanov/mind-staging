%h1 Welcome to the MIND Analytics Platform
#user_log_in
  %p
    Please login below.
    %br
    If you need access please contact: jason@marketingscience.co.

  %h1 log in

  = form_for @user = User.new, as: :user,
                       url: user_session_path do |f|     
    = f.fields_for :accounts do |account_form|
      .field
        = account_form.label :account
        = account_form.text_field :name, autofocus: true                                                                 
    .field
      = f.label :email
      %br
      = f.text_field :email
    .field
      = f.label :password
      %br
      = f.password_field :password
    .actions
      = f.submit 'log in'